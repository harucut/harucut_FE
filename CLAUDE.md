# CLAUDE.md

**먼저 [AGENTS.md](./AGENTS.md) 를 읽는다.** 이 저장소의 규칙 원본은 그 파일 하나다 —
저장소 구조, 규칙의 소유자 표, 라우트, 브랜치·커밋 규칙, 검증 명령, CI 라벨 규칙이 전부 거기 있다.

여기에는 **그것을 못 읽었을 때 틀리면 안 되는 것**만 적는다. 나머지를 여기 복사하지 않는다 —
두 파일이 갈라지는 것이 이 파일을 만든 이유다.

## 틀리면 안 되는 것

1. **앱은 자기 화면을 그리지 않는다.** `apps/mobile` 은 웹을 띄우는 WebView 셸이고 소스가
   4개 파일, 라우트가 1개다(ADR-0003, 2026-08-18 채택).
   **"모바일 화면을 고쳐라"의 정답은 거의 항상 `apps/web` 이다.**
   expo-router 라우트를 새로 만들지 않는다 — ADR-0003 을 되돌리는 일이다.

2. **네이티브가 맡는 것은 다섯 가지뿐이다** — 사진첩 저장 · 공유 시트 · 햅틱 ·
   하드웨어 뒤로가기 · `harucut://` 딥링크. 목록의 소유자는
   [`docs/adr/ADR-0003-앱을-웹뷰-셸로.md`](docs/adr/ADR-0003-앱을-웹뷰-셸로.md) 「경계선」 절.

3. **브리지는 한 쌍이다** — `apps/web/lib/nativeBridge.ts` ↔ `apps/mobile/lib/native-bridge.ts`.
   한쪽만 고치면 프로토콜이 갈라진다. 같은 커밋에서 둘을 고친다.

4. **인증 흐름을 검증할 때 `NEXT_PUBLIC_DEV_AUTH_BYPASS` 를 끈다.** 켜져 있으면
   `apps/web/proxy.ts:51-53` 이 보호 경로 판정을 통째로 건너뛰어 인증 e2e 가 조용히 통과한다.

5. **브랜치는 `<type>/<slug>`, 커밋은 `<type>(<scope>): 요약`, PR 제목은 `<type>: 요약`.**
   `main`·`develop`·`develop_loop` 에 직접 commit/push 하지 않는다.

6. **직접 확인하지 않은 기능을 완료라고 쓰지 않는다.** 응답은 한국어, 정리형, 짧은 문장.

## 조사 전에

[`docs/README.md`](docs/README.md) 를 먼저 연다. 문서 지도와 _이미 확인이 끝난 사실_(계약 대조
결과, 이미 정상이라 손대지 않은 것, 백엔드 답을 기다리는 것)이 거기 있다.
