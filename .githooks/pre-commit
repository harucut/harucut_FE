#!/usr/bin/env bash
set -euo pipefail

branch="$(git symbolic-ref --short HEAD 2>/dev/null || true)"

case "$branch" in
  main|develop|develop_loop)
    echo "ERROR: commits on $branch are forbidden. Use issue/<number>-<slug> branches."
    exit 1
    ;;
esac
